export interface News {
    title: string
    description: string
    authorWords: string
    vote: number
    _id: string
    wikipediaUrl: string
    author: string
    createdAt: string
    updatedAt: string
}
